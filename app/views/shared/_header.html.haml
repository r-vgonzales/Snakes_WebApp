%h1 The Snake Network
#mySidebar.sidebar
 %a.closebtn{:href => "javascript:void(0)", :onclick => "closeNav()"} ×
 %a{:href => "/"} Home
 %a{:href => "/addsnake"} Add Your Snake
 %a{:href => "/allsnakes"} All Snake Pets
 %a{:href => "/facts"} Facts
 %a{:href => "/contact"} Contact Us
#main
 %button.openbtn{:onclick => "openNav()"} ≡ Open Sidebar

%nav
 - if user_signed_in?
  = "#{current_user.email} |"
  = link_to t('profile'), edit_user_registration_path
  = '|'
  = link_to t('logout'), destroy_user_session_path, method: 'delete'
 - else
  = link_to t('login'), new_user_session_path
  = '|'
  = link_to t('sign_up'), new_user_registration_path

- flash.each do |key, value|
 = content_tag :div, content_tag(:p, value), id: "#{key}" unless value.blank?
